# Placeholder defaults for the ml-gradle plugin. mlshMlcp always sets real
# connection details explicitly, so these values are never actually used to
# reach a MarkLogic server; they only satisfy ml-gradle's property lookups.
mlAppName=mlsh-mlcp-runner
mlHost=localhost
mlRestPort=8000
mlUsername=admin
mlPassword=admin
